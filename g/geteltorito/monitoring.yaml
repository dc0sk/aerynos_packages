# SPDX-FileCopyrightText: 2026 AerynOS Developers
# SPDX-License-Identifier: MPL-2.0

releases:
  # https://release-monitoring.org/project/231582/ (GitHub backend). Upstream
  # publishes no tags or releases, so there is nothing for this to match on;
  # the recipe tracks a pinned commit instead.
  id: 231582
  rss: ~
security:
  cpe: ~ # No known CPE. Last checked 20260812
